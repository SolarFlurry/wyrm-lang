func hello(a: *mut i32, b: *i32, c: [i32], d: [mut i32], e: [*]i32, f: [*mut]i32) {
	// a is mutable
	// b is immutable
	// c is immutable
	// d is mutable
	// e is immutable
	// f is mutable
}

pub func main() {
	let mut x = 0;
	let mut y = [0; 10];
	hello(mut &x, &x, y[6..], mut y[6..], mut &y, mut &y);
}